---
phase: 01-app-shell-and-local-data-backbone
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - PomodoroMac/App/PomodoroMacApp.swift
  - PomodoroMac/App/AppEnvironment.swift
  - PomodoroMac/App/Bootstrap/AppBootstrapper.swift
  - PomodoroMac/App/Bootstrap/BootstrapState.swift
  - PomodoroMac/App/Bootstrap/BootstrapRootView.swift
  - PomodoroMac/App/Bootstrap/BootstrapDiagnostics.swift
  - PomodoroMac/Features/Dashboard/DashboardView.swift
  - PomodoroMac/Infrastructure/Persistence/ModelContainerFactory.swift
  - PomodoroMac/Infrastructure/Settings/UserDefaultsSettingsStore.swift
  - PomodoroMacTests/Bootstrap/AppBootstrapperTests.swift
  - PomodoroMacTests/Bootstrap/RelaunchRecoveryTests.swift
autonomous: true
requirements:
  - DASH-01
  - CONF-03
  - CONF-04
user_setup: []
must_haves:
  truths:
    - App launch completes from local disk state alone, with no network dependency or remote bootstrap step.
    - The app registers defaults, opens the shared local store, constructs repositories/settings, and lands on Dashboard even with zero stored data.
    - Representative user data written before termination is visible again after recreating the full bootstrap pipeline, not just the raw container.
    - A failed store bootstrap produces a recoverable error surface and diagnostics instead of a crash.
    - Relaunch foundations exist for future active-session recovery through a persisted snapshot path, even before the timer UI is implemented.
  artifacts:
    - PomodoroMac/App/AppEnvironment.swift encapsulates repositories, settings, and bootstrap state for the app shell.
    - PomodoroMac/App/Bootstrap/AppBootstrapper.swift performs the ordered offline-safe startup pipeline.
    - PomodoroMacTests/Bootstrap/AppBootstrapperTests.swift and PomodoroMacTests/Bootstrap/RelaunchRecoveryTests.swift cover success, restart-durability, and failure bootstrap paths.
  key_links:
    - PomodoroMac/App/PomodoroMacApp.swift uses AppBootstrapper to provide AppEnvironment to the shell from Plan 01.
    - ModelContainerFactory and UserDefaultsSettingsStore are composed during bootstrap rather than being constructed ad hoc inside views.
    - Bootstrap tests recreate the full AppBootstrapper against the same store location to prove persisted user data survives relaunch.
    - BootstrapRootView surfaces either the shell or a recoverable failure state based on BootstrapState.
---

<objective>
Wire the app shell and data backbone into an offline-safe startup pipeline that can launch cleanly, recover from local bootstrap failures, and preserve the seam for future relaunch-safe timer recovery.

Purpose: Turn the Phase 1 shell and persistence work into a usable local-first application foundation that restarts reliably and does not depend on internet access or placeholder seed data.
Output: Bootstrap/environment wiring, local-store startup sequence, recoverable failure state, relaunch snapshot plumbing, and bootstrap integration tests.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-app-shell-and-local-data-backbone/01-CONTEXT.md
@.planning/phases/01-app-shell-and-local-data-backbone/01-RESEARCH.md
@.planning/phases/01-app-shell-and-local-data-backbone/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the ordered bootstrap pipeline and shared app environment</name>
  <files>PomodoroMac/App/PomodoroMacApp.swift, PomodoroMac/App/AppEnvironment.swift, PomodoroMac/App/Bootstrap/AppBootstrapper.swift, PomodoroMac/App/Bootstrap/BootstrapState.swift, PomodoroMac/App/Bootstrap/BootstrapDiagnostics.swift, PomodoroMac/Infrastructure/Persistence/ModelContainerFactory.swift, PomodoroMac/Infrastructure/Settings/UserDefaultsSettingsStore.swift</files>
  <action>Create an `AppBootstrapper` that performs startup in a deterministic order: register default settings, open the local SwiftData container in Application Support, construct repository/services, inspect any persisted active-session snapshot, and expose the result through an `AppEnvironment`. Keep bootstrap free of network calls, remote configuration, or permissions gates. Preserve enough control over the store location and settings scope that tests can write representative user data, tear down the bootstrap pipeline, recreate it against the same backing stores, and prove relaunch durability. Add structured diagnostics with `OSLog` or equivalent so store creation and recovery paths are inspectable without crashing the app.</action>
  <verify>`xcodebuild build -scheme PomodoroMac -destination 'platform=macOS'`</verify>
  <done>The app has one bootstrap authority for local startup, views no longer need to create persistence/settings dependencies for themselves, and bootstrap can be recreated against the same local stores for restart-durability verification.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate bootstrap state into the shell and add recoverable empty/failure launch handling</name>
  <files>PomodoroMac/App/Bootstrap/BootstrapRootView.swift, PomodoroMac/Features/Dashboard/DashboardView.swift, PomodoroMac/App/PomodoroMacApp.swift</files>
  <action>Render a root bootstrap view that transitions into the sidebar shell when local dependencies load successfully and shows a recoverable failure state when they do not. Preserve the Dashboard-first experience from Plan 01, keep the no-data state honest, and surface a small recovery action or restart guidance instead of fatal errors. If an active-session snapshot exists, expose it through bootstrap state for later timer recovery work, but do not implement timer UI or state restoration beyond the foundation seam in this phase.</action>
  <verify>`xcodebuild build -scheme PomodoroMac -destination 'platform=macOS'`</verify>
  <done>Cold launch still lands on Dashboard when bootstrap succeeds, empty data does not block the shell, and local bootstrap failure has a visible non-crashing fallback path.</done>
</task>

<task type="auto">
  <name>Task 3: Add offline and relaunch-oriented bootstrap integration tests</name>
  <files>PomodoroMacTests/Bootstrap/AppBootstrapperTests.swift, PomodoroMacTests/Bootstrap/RelaunchRecoveryTests.swift</files>
  <action>Add tests that validate default-settings registration, production-store path selection, successful environment construction, recoverable container creation failure, and loading of a persisted active-session snapshot into bootstrap state. Include a full restart-durability case that writes representative user data through repositories and settings, tears down the environment, recreates the bootstrap pipeline against the same backing stores, and proves the data is still present after relaunch. Keep the tests deterministic by stubbing file locations and container outcomes where needed. The tests should prove that startup depends only on local state and remains ready for Phase 3 relaunch-safe timer work.</action>
  <verify>`xcodebuild test -scheme PomodoroMac -destination 'platform=macOS'`</verify>
  <done>Automated coverage exists for happy-path startup, restart durability across bootstrap recreation, failure fallback, and relaunch foundation behavior, and those tests fail if bootstrap regresses into crash-only, non-durable, or network-dependent logic.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild build -scheme PomodoroMac -destination 'platform=macOS'`
- [ ] `xcodebuild test -scheme PomodoroMac -destination 'platform=macOS'`
- [ ] Automated tests prove representative user data survives full bootstrap recreation against the same local stores
- [ ] Manual offline cold launch confirms the app opens directly to Dashboard with network disabled
- [ ] Manual failure-path check confirms a broken local store surfaces recovery UI instead of terminating the app
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Startup is fully local-first and does not require internet connectivity
- Dashboard remains the launch landing page after the bootstrap pipeline is introduced
- Representative persisted user data survives relaunch, proven through write -> recreate bootstrap -> read-back verification
- The codebase contains a recoverable relaunch foundation for future active-session restoration work

</success_criteria>

<output>
After completion, create `.planning/phases/01-app-shell-and-local-data-backbone/01-03-SUMMARY.md`
</output>
