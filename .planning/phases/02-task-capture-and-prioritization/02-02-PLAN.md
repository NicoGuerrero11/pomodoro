---
phase: 02-task-capture-and-prioritization
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - PomodoroMac/App/Shell/AppShellView.swift
  - PomodoroMac/Features/Dashboard/DashboardView.swift
  - PomodoroMac/Features/Tasks/TasksWorkspaceView.swift
  - PomodoroMac/Features/Tasks/TasksFeatureModel.swift
  - PomodoroMac/Features/Tasks/TaskListPaneView.swift
  - PomodoroMac/Features/Tasks/TaskEditorPaneView.swift
  - PomodoroMacTests/Tasks/TasksWorkspaceTests.swift
autonomous: true
requirements:
  - TASK-01
  - TASK-02
  - TASK-03
user_setup: []
must_haves:
  truths:
    - The Tasks section replaces its placeholder with a real master-detail workspace that feels native inside the existing macOS shell.
    - The empty Tasks state uses a focused empty pane with short, direct copy and a primary action that opens a blank task form immediately.
    - The dashboard's existing "Create First Task" action routes to the Tasks section and lands in blank task creation without extra clicks.
    - Selecting a task opens it directly in editable form with title, description, and priority visible at once.
    - Create and edit flows use explicit Save and Cancel behavior rather than autosave.
    - The task list shows title, visible priority, and short description preview while reflecting the repository’s priority-first ordering.
  artifacts:
    - PomodoroMac/Features/Tasks/TasksWorkspaceView.swift owns the real Tasks section UI.
    - PomodoroMac/Features/Tasks/TasksFeatureModel.swift (or equivalent feature state type) handles selection, drafts, save/cancel, and dashboard handoff logic.
    - PomodoroMacTests/Tasks/TasksWorkspaceTests.swift covers empty-state entry, selection/edit behavior, and explicit save/cancel flow.
  key_links:
    - AppShellView should route `.tasks` to the real feature workspace instead of placeholder copy.
    - DashboardView should use the existing CTA to trigger the blank task-creation handoff into the Tasks section.
    - The Tasks UI must consume repository-backed ordered tasks from Plan 02-01 rather than sorting independently.
---

<objective>
Ship the first real Tasks experience by replacing the placeholder page with a master-detail workspace for creating, editing, and browsing pending tasks in priority order.

Purpose: Turn the Phase 2 product decisions into a usable day-one task flow inside the existing shell, including the first-task onboarding handoff from the dashboard.
Output: Real Tasks section UI, explicit create/edit interactions, dashboard handoff, and tests for the chosen workspace behavior.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-task-capture-and-prioritization/02-CONTEXT.md
@.planning/phases/02-task-capture-and-prioritization/02-RESEARCH.md
@.planning/phases/02-task-capture-and-prioritization/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the task feature state and entry flows for creation and selection</name>
  <files>PomodoroMac/Features/Tasks/TasksFeatureModel.swift, PomodoroMac/Features/Dashboard/DashboardView.swift, PomodoroMac/App/Shell/AppShellView.swift</files>
  <action>Introduce the feature-level state needed for the Tasks workspace: loading ordered pending tasks from the repository, opening a blank creation draft, selecting an existing task for editing, and discarding or saving edits explicitly. Wire the dashboard CTA so "Create First Task" routes into the Tasks section and lands in the blank form immediately. Keep the interaction model aligned with the context decisions: in-shell, explicit save/cancel, and no modal-first workflow.</action>
  <verify>`xcodebuild build -scheme PomodoroMac -destination 'platform=macOS'`</verify>
  <done>The app has one coherent feature-state path for task creation and editing, and the dashboard-to-task handoff is real instead of placeholder navigation.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the Tasks placeholder with the real master-detail workspace UI</name>
  <files>PomodoroMac/Features/Tasks/TasksWorkspaceView.swift, PomodoroMac/Features/Tasks/TaskListPaneView.swift, PomodoroMac/Features/Tasks/TaskEditorPaneView.swift, PomodoroMac/App/Shell/AppShellView.swift</files>
  <action>Build the real Tasks section UI using the chosen interaction model. The empty state should be a focused pane with short/direct copy and a primary action to open the blank editor. Once tasks exist, show a compact list with title, visible priority, and short description preview beside the editor/detail pane. Selecting a row should open that task in editable form immediately. Use a toolbar create action for additional tasks, keep the layout Mac-native, and avoid adding completion views, filters, or extra scope.</action>
  <verify>`xcodebuild build -scheme PomodoroMac -destination 'platform=macOS'`</verify>
  <done>The Tasks section is now a real feature workspace rather than a placeholder, with the list/detail behavior and first-use flow locked in by the context decisions.</done>
</task>

<task type="auto">
  <name>Task 3: Add feature tests for empty-state entry, selection/editing, and explicit save/cancel behavior</name>
  <files>PomodoroMacTests/Tasks/TasksWorkspaceTests.swift</files>
  <action>Add focused XCTest coverage for the Phase 2 workspace behavior. Verify that the empty Tasks state opens a blank task form from its CTA, the dashboard handoff can trigger the same creation flow, selecting an existing task opens it for editing, and explicit Save/Cancel produces the right persisted outcome. Keep the tests at the feature-state/workspace level where possible rather than trying to snapshot every visual detail.</action>
  <verify>`xcodebuild test -scheme PomodoroMac -destination 'platform=macOS'`</verify>
  <done>Automated tests fail if the first-task entry flow breaks, if selection stops opening editable task details, or if Save/Cancel semantics drift away from the chosen interaction model.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild build -scheme PomodoroMac -destination 'platform=macOS'`
- [ ] `xcodebuild test -scheme PomodoroMac -destination 'platform=macOS'`
- [ ] Automated tests prove the empty Tasks state opens a blank editor
- [ ] Automated tests prove explicit Save/Cancel behavior for task edits
- [ ] Manual launch confirms the Tasks section feels like a native master-detail workspace
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The Tasks section is genuinely usable for day-one task creation and editing
- The dashboard CTA hands off directly into the first-task flow
- Priority-first ordering is visible in the user-facing task list, not just in storage tests

</success_criteria>

<output>
After completion, create `.planning/phases/02-task-capture-and-prioritization/02-02-SUMMARY.md`
</output>
