---
phase: 02-task-capture-and-prioritization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - PomodoroMac/Domain/Models/TaskItem.swift
  - PomodoroMac/Infrastructure/Persistence/Models/TaskRecord.swift
  - PomodoroMac/Infrastructure/Persistence/Repositories/TaskRepository.swift
  - PomodoroMacTests/Tasks/TaskRepositoryTests.swift
autonomous: true
requirements:
  - TASK-01
  - TASK-02
  - TASK-03
user_setup: []
must_haves:
  truths:
    - Task creation persists title, description, and priority through the existing repository boundary without exposing raw SwiftData APIs to the Tasks UI.
    - Task editing updates the persisted task in place rather than creating duplicates, and preserves task identity plus original creation timestamp.
    - Pending tasks are returned in deterministic priority-first order so higher-priority tasks always appear before lower-priority tasks.
    - Task validation rejects unusable persisted state such as blank titles, leaving the UI with one consistent source of truth for create/edit rules.
  artifacts:
    - PomodoroMac/Infrastructure/Persistence/Repositories/TaskRepository.swift exposes the Phase 2 CRUD and ordered-query seams the UI will call.
    - PomodoroMacTests/Tasks/TaskRepositoryTests.swift proves create, edit, validation, and priority-first ordering behavior.
  key_links:
    - The Tasks workspace in Plan 02-02 should consume repository-backed ordered pending tasks instead of sorting ad hoc in the view layer.
    - Repository APIs from this plan should be sufficient for both empty-state creation and editing an existing selected task from the list.
---

<objective>
Harden the task data seam for Phase 2 by making create, edit, validation, and priority-first ordering explicit before any real Tasks UI is built.

Purpose: Give the UI plan a deterministic, test-backed task feature contract so the first Tasks workspace can stay thin and focus on interaction design rather than persistence correctness.
Output: Repository-facing create/edit/query rules plus tests that prove pending tasks order correctly and invalid task data is rejected.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-task-capture-and-prioritization/02-CONTEXT.md
@.planning/phases/02-task-capture-and-prioritization/02-RESEARCH.md
@.planning/phases/02-task-capture-and-prioritization/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Formalize Phase 2 task rules at the domain and repository seam</name>
  <files>PomodoroMac/Domain/Models/TaskItem.swift, PomodoroMac/Infrastructure/Persistence/Repositories/TaskRepository.swift</files>
  <action>Review the existing task model and repository contract against the Phase 2 requirements and context. Make the create/edit expectations explicit for this phase: tasks need a usable title, editable description, editable priority, and a repository query that returns pending tasks in priority-first order. Add any small contract changes needed so the UI can create a new task draft, save it, edit an existing task, and request the canonical ordered list without doing its own business logic. Keep the phase scoped to pending-task CRUD only; do not pull in completion workflows, tags, subtasks, or timer-linking behavior.</action>
  <verify>`xcodebuild build -scheme PomodoroMac -destination 'platform=macOS'`</verify>
  <done>The codebase has an explicit repository/service contract for Phase 2 task create/edit/list behavior, and the ordering/validation rules the UI will rely on are no longer implicit.</done>
</task>

<task type="auto">
  <name>Task 2: Implement repository-backed create, edit, validation, and ordered pending-task queries</name>
  <files>PomodoroMac/Infrastructure/Persistence/Models/TaskRecord.swift, PomodoroMac/Infrastructure/Persistence/Repositories/TaskRepository.swift</files>
  <action>Implement the repository behavior needed for Phase 2: creating a task, updating an existing task, and fetching pending tasks in deterministic priority-first order. If validation belongs in a helper or small domain-level rule, add it where it keeps the repository/UI boundary clean. Make sure edits update the correct record rather than creating duplicates, and that any secondary sort remains stable so equal-priority items behave predictably. Keep persistence concerns behind the repository and avoid view-specific state or SwiftUI types in this plan.</action>
  <verify>`xcodebuild build -scheme PomodoroMac -destination 'platform=macOS'`</verify>
  <done>The repository can reliably power the first Tasks UI: invalid data is rejected, updates are in-place, and pending tasks come back ordered so the user sees high-priority work first.</done>
</task>

<task type="auto">
  <name>Task 3: Add repository tests for task creation, editing, validation, and priority order</name>
  <files>PomodoroMacTests/Tasks/TaskRepositoryTests.swift</files>
  <action>Add focused XCTest coverage proving the Phase 2 repository seam works as promised. At minimum, test that creating a task persists the expected fields, editing updates the stored task instead of duplicating it, invalid task input is rejected, and a mixed set of pending tasks is returned in priority-first order. Use the same local test container approach established in Phase 1 so these tests stay deterministic and fast.</action>
  <verify>`xcodebuild test -scheme PomodoroMac -destination 'platform=macOS'`</verify>
  <done>Automated tests fail if task create/edit behavior regresses, if invalid tasks can be persisted, or if task ordering stops reflecting priority-first rules.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild build -scheme PomodoroMac -destination 'platform=macOS'`
- [ ] `xcodebuild test -scheme PomodoroMac -destination 'platform=macOS'`
- [ ] Automated tests prove pending tasks return in priority-first order
- [ ] Automated tests prove editing updates the existing task rather than inserting a duplicate
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The Tasks feature has a repository contract strong enough to support create/edit/list behavior without UI-owned business rules
- Priority-first ordering is deterministic and test-backed
- Validation rules for task creation/editing are explicit and reusable in later UI work

</success_criteria>

<output>
After completion, create `.planning/phases/02-task-capture-and-prioritization/02-01-SUMMARY.md`
</output>
