---
phase: 01-app-shell-and-local-data-backbone
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - PomodoroMac.xcodeproj/project.pbxproj
  - PomodoroMac/App/PomodoroMacApp.swift
  - PomodoroMac/App/Navigation/AppSection.swift
  - PomodoroMac/App/Navigation/AppRouter.swift
  - PomodoroMac/App/Shell/AppShellView.swift
  - PomodoroMac/Features/Dashboard/DashboardView.swift
  - PomodoroMac/Features/Shared/SectionPlaceholderView.swift
  - PomodoroMac/Features/Shared/SidebarRowLabel.swift
  - PomodoroMacTests/Navigation/AppShellNavigationTests.swift
autonomous: true
requirements:
  - DASH-01
  - CONF-05
user_setup: []
must_haves:
  truths:
    - Cold launch opens the main window with Dashboard selected as the active destination.
    - The sidebar shows Dashboard, Timer, Tasks, Statistics, History, and Settings using standard macOS split-view behavior.
    - Non-Dashboard sections render real placeholder pages with calm explanatory copy instead of missing routes or teaser content.
  artifacts:
    - PomodoroMac.xcodeproj/project.pbxproj defines a macOS SwiftUI app target and XCTest target.
    - PomodoroMac/App/Shell/AppShellView.swift renders the root NavigationSplitView shell.
    - PomodoroMac/Features/Dashboard/DashboardView.swift provides the timer-first empty-state dashboard.
  key_links:
    - PomodoroMac/App/PomodoroMacApp.swift launches AppShellView as the initial WindowGroup content.
    - PomodoroMac/App/Navigation/AppSection.swift and PomodoroMac/App/Navigation/AppRouter.swift drive sidebar selection and detail routing.
    - PomodoroMacTests/Navigation/AppShellNavigationTests.swift proves the default route and all sidebar destinations exist.
---

<objective>
Establish the first launchable macOS app shell, including the Xcode project scaffold, the sidebar-driven NavigationSplitView shell, and the honest Dashboard-first landing flow.

Purpose: Give later Phase 1 work a stable native shell to build on while satisfying the v1 navigation contract from the first executable plan.
Output: A macOS SwiftUI app target, root shell/navigation files, Dashboard empty state, section placeholders, and shell navigation tests.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-app-shell-and-local-data-backbone/01-CONTEXT.md
@.planning/phases/01-app-shell-and-local-data-backbone/01-RESEARCH.md
@.planning/phases/01-app-shell-and-local-data-backbone/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the native macOS SwiftUI project scaffold and root app entry</name>
  <files>PomodoroMac.xcodeproj/project.pbxproj, PomodoroMac/App/PomodoroMacApp.swift</files>
  <action>Create the greenfield macOS SwiftUI app target and XCTest target for `PomodoroMac`, set the deployment target to macOS 14+, and wire a single `WindowGroup` root entry that can host the sidebar shell. Keep the app entry intentionally thin so later plans can inject bootstrap and persistence state without rewriting scene structure. Do not add sample content, onboarding flows, or separate Settings windows in this plan.</action>
  <verify>`xcodebuild build -scheme PomodoroMac -destination 'platform=macOS'`</verify>
  <done>The repo contains a compilable native macOS app target plus test target, and `PomodoroMacApp` launches a dedicated shell view instead of inline demo UI.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the sidebar shell and Dashboard-first detail experience</name>
  <files>PomodoroMac/App/Navigation/AppSection.swift, PomodoroMac/App/Navigation/AppRouter.swift, PomodoroMac/App/Shell/AppShellView.swift, PomodoroMac/Features/Dashboard/DashboardView.swift, PomodoroMac/Features/Shared/SectionPlaceholderView.swift, PomodoroMac/Features/Shared/SidebarRowLabel.swift</files>
  <action>Define a stable `AppSection` enum and router object for the six v1 destinations, then render them through `NavigationSplitView` with Dashboard selected by default on cold launch. Build a timer-first Dashboard empty state with a primary focus card, a short getting-started checklist, and honest empty supporting cards. For Timer, Tasks, Statistics, History, and Settings, create real placeholder views with concise calm copy describing what will appear later. Use standard SF Symbols and native sidebar list patterns; do not invent custom chrome, preview metrics, or restore the last visited section.</action>
  <verify>`xcodebuild build -scheme PomodoroMac -destination 'platform=macOS'`</verify>
  <done>Launching the app shows Dashboard by default, the sidebar exposes all six sections, and every destination renders a real page with native split-view navigation behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Add shell navigation tests for default landing and section coverage</name>
  <files>PomodoroMacTests/Navigation/AppShellNavigationTests.swift</files>
  <action>Add XCTest coverage that exercises the shell routing surface instead of relying on manual inspection alone. Verify the `AppSection` cases match the roadmap sections, the initial router selection is `.dashboard`, and the shell can construct placeholder destinations for all non-Dashboard pages without missing cases. Keep the tests narrow and stable so later feature work can extend them rather than replace them.</action>
  <verify>`xcodebuild test -scheme PomodoroMac -destination 'platform=macOS'`</verify>
  <done>Automated tests fail if Dashboard stops being the default route or if any required sidebar section disappears from the routing model.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild build -scheme PomodoroMac -destination 'platform=macOS'`
- [ ] `xcodebuild test -scheme PomodoroMac -destination 'platform=macOS'`
- [ ] Manual launch confirms Dashboard opens first and sidebar collapse/show uses standard macOS split-view behavior
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The app shell is launchable as a native macOS SwiftUI target
- Dashboard is the only default cold-launch destination in the shell
- The sidebar contract for Dashboard, Timer, Tasks, Statistics, History, and Settings exists in code and tests

</success_criteria>

<output>
After completion, create `.planning/phases/01-app-shell-and-local-data-backbone/01-01-SUMMARY.md`
</output>
